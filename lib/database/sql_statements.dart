const createTaskTableCommand =
    "CREATE TABLE Tasks ( id  INTEGER PRIMARY KEY  AUTOINCREMENT NOT NULL UNIQUE,  title  TEXT, content TEXT, due_date TEXT,  status TEXT,  created_at TEXT,   updated_at TEX);";
